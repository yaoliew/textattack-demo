# Core dependencies for textattack-demo project
# Install with: pip install -r requirements.txt

# TextAttack - for adversarial text attack demo (main.py)
textattack>=0.3.8

# Transformers - Qwen2.5-VL classes require newer versions
transformers>=4.48.0

# PyTorch - for model inference (both scripts)
# Note: Install appropriate version for your system (CPU or CUDA)
# For CPU: pip install torch
# For CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# Accelerate - for efficient model loading (qwen_demo.py with device_map="auto")
accelerate>=0.20.0

# Progress bar (used in main.py, may be included with textattack)
tqdm>=4.65.0

# Additional dependencies used by metrics and demos
numpy>=1.24.0
datasets>=2.12.0
scikit-learn>=1.2.0
requests>=2.31.0
python-dotenv>=1.0.0
python-whois>=0.9.0
Pillow>=9.5.0
langdetect>=1.0.9
openai>=1.0.0

# TensorFlow is required for TextBugger/BAE/CLARE attack paths in test_qwen_smishing.py.
# CPU execution for TF is configured in-script to avoid GPU JIT issues.
tensorflow>=2.12.0

